{
  "name": "gestion-proyecto-api",
  "version": "1.0.0",
  "description": "API Backend - Cloudflare Workers + Supabase",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:staging": "wrangler deploy --env development",
    "deploy:production": "wrangler deploy --env production",
    "logs": "wrangler tail",
    "init-db": "python app.py"
  },
  "keywords": ["cloudflare", "workers", "supabase", "api"],
  "author": "Tu nombre",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
